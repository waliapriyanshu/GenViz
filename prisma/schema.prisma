datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Data {
  id       Int    @id @default(autoincrement())
  product  String
  category String
  revenue  Int
  quantity Int
  region   String
}
